<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="Vuln07 NavHijack"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                 <nav-graph> автоматически добавляет в Activity
                 intent-filter'ы для ВСЕХ <deepLink> объявленных в
                 nav_graph.xml. Это и есть «уязвимая навигация» —
                 любой deeplink из графа становится точкой входа,
                 минуя стартовую destination (LoginFragment).
            -->
            <nav-graph android:value="@navigation/nav_graph" />
        </activity>
    </application>
</manifest>
